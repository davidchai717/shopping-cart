const fs = require('fs');
const path = require('path');
const items = require('../models/items');

console.log(items);
const carts = [];

module.exports = {
  addCart: {
    // access from the store
    // create a new Cart entry
    // return cart ID
  },
  getItems: {
  },
  addItem: {

  },
}